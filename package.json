{
    "dependencies": {
        "ataraxia": "^0.12.2",
        "ataraxia-tcp": "^0.12.0",
        "dotenv": "^16.0.3"
    },
    "scripts": {
        "build": "tsc --build --verbose",
        "build-dev": "rimraf ./build && tsc --build --verbose"
    },
    "devDependencies": {
        "ts-node": "^10.9.1",
        "typechain": "^8.1.1",
        "typescript": "^4.9.5",
        "rimraf": "^4.1.2",
        "prettier": "^2.8.4"
    }
}
